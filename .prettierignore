# Build output and dependencies
dist
node_modules
coverage

# The macOS target's copy of dist/, regenerated by scripts/build-macos.sh
macos/PlanEditorMac/Sources/PlanEditorMac/WebApp

# package-lock.json is written by npm; reformatting it only creates conflicts
package-lock.json

# Prose. The docs are hand-wrapped and hand-reviewed; Prettier rewrites
# *emphasis* as _emphasis_ and drops the indentation that keeps a wrapped
# line inside its list item — churn on files no formatter makes better.
*.md
